{
  "name": "chatbot-webhook",
  "version": "1.0.0",
  "description": "Webhook para DialogFlow conectado a MySQL",
  "main": "netlify/functions/webhook.js",
  "scripts": {
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "mysql2": "^3.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
